apiVersion: apps/v1
kind: Deployment
metadata:
  name: annotation-api
spec:
  template:
    spec:
      containers:
        - name: annotation-api
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 4Gi
