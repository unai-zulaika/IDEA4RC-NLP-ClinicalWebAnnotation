apiVersion: apps/v1
kind: Deployment
metadata:
  name: annotation-api
spec:
  template:
    spec:
      containers:
        - name: annotation-api
          env:
            - name: CORS_ORIGINS
              value: "https://clinical.example.com"
