apiVersion: v1
kind: Service
metadata:
  name: pipeline-dashboard
  labels:
    app.kubernetes.io/name: pipeline-dashboard
    app.kubernetes.io/component: pipeline
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pipeline-dashboard
  ports:
    - port: 8501
      targetPort: 8501
      protocol: TCP
      name: http
